core_connect:
    host: localhost
    protocol: "http"
    port: "9050"
    tamr_username: my_user
    tamr_password: my_password
    jdbc:
        ingest:
            jdbc_url: tamr::jdbc_ingest
            db_user: ingest_user
            db_password: ingest_pw
            fetch_size: 10000